package com.targetindia.pack2;

import com.targetindia.pack1.ClassA;

public class ClassC {
    public static void main(String args[])
    {
        ClassC c=new ClassC();
        /*c.packagefun();//new package so protected private cannot be accessed in java default access specifier is private
        c.privatefun();
        c.protectedfun();
        c.publicfun();//public can be accessed anywhere
         */
    }
}